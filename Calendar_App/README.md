# How to Run the Flask App

1. Make sure Python 3 is installed on your system (Python 3.8+ recommended).
2. Install Flask if you don’t already have it:
```bash
pip install flask
```
4. Open Command Prompt.
5. Navigate to the folder where app.py is located:
`cd path/to/your/project`.
7. Run the application in the terminal:
```bash
python app.py
```
8. After running, the terminal will show:
   Running on
 ```bash
http://127.0.0.1:5000/
 ```
10. Open your web browser and go to that URL to view the app.
